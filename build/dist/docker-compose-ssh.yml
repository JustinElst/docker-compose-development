services:
  php74:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro
  php81:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro
  php82:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro
  php83:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro