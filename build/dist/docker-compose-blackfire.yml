services:
  blackfire:
    env_file:
      - ./conf/blackfire
    build: build/dist/blackfire

  php81:
    environment:
      - "BLACKFIRE_AGENT_SOCKET=tcp://blackfire:8307"
  php82:
    environment:
      - "BLACKFIRE_AGENT_SOCKET=tcp://blackfire:8307"

